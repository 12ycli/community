<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyc.community.mapper.QuestionMapperExtension">
  <resultMap id="BaseResultMap" type="com.lyc.community.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Wed Oct 09 20:10:58 CST 2019.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
    <result column="creator" jdbcType="BIGINT" property="creator" />
    <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
    <result column="view_count" jdbcType="INTEGER" property="viewCount" />
    <result column="like_count" jdbcType="INTEGER" property="likeCount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
  </resultMap>

  <update id="increaseViewCount" parameterType="long">
    update question set view_count = view_count + 1 where id = #{id}
  </update>

  <update id="increaseCommentCount" parameterType="long">
    update question set comment_count = comment_count + 1 where id = #{id}
  </update>

  <select id="countBySearch" parameterType="com.lyc.community.dto.QuestionQueryDTO" resultType="int">
    select count(*) from question
    <where>
      <if test="search!=null">
        and title regexp #{search}
      </if>
    </where>
  </select>

<!--  List<Question> -->
  <select id="selectBySearch" parameterType="com.lyc.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from question
    <where>
      <if test="search!=null">
        and title regexp #{search}
      </if>
    </where>
    order by gmt_create desc limit #{page},#{size}
  </select>
</mapper>
